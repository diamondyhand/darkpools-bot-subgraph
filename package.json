{
  "name": "bstar0406-subgraph-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ bstar0406-subgraph-test",
    "create-local": "graph create --node http://localhost:8020/ bstar0406-subgraph-test",
    "remove-local": "graph remove --node http://localhost:8020/ bstar0406-subgraph-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bstar0406-subgraph-test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.2"
  }
}
